[project]
name = "linkedin-app-langchain-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ali Soltani", email = "ali.soltani@red-gate.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.4.0",
    "langchain-anthropic>=1.7.1",
]

[project.scripts]
linkedin-app-langchain-agent = "linkedin_app_langchain_agent:main"

[build-system]
requires = ["uv_build>=0.10.0,<0.11.0"]
build-backend = "uv_build"
