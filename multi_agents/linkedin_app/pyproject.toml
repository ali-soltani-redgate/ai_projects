[project]
name = "linkedin-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ali Soltani", email = "ali.soltani@red-gate.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
linkedin-app = "linkedin_app:main"

[build-system]
requires = ["uv_build>=0.10.0,<0.11.0"]
build-backend = "uv_build"
